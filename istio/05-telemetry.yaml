apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: tracing-only
spec:
  # SOLO configuración de trazas - NO tocar métricas
  tracing:
  - randomSamplingPercentage: 100.0
    providers:
    - name: jaeger