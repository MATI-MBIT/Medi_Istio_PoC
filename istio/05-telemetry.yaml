apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: tracing-only
spec:
  # Configuración básica de trazas (filtros se manejan en EnvoyFilter)
  tracing:
  - randomSamplingPercentage: 100.0
    providers:
    - name: jaeger